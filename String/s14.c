#include <stdio.h>
#include <string.h>

void checkChar(char str[], char ch);

int main (){
    char str[] = "kaif";
    char ch = 'o';
    checkChar(str,ch);
}
void checkChar(char str[], char ch){
for(int i=0; str[i] !='\0'; i++){
    if(str[i] = 'n'){
        printf("character is present!");
    

 return ;
    }
            }
printf("charcharter is NOT present :");
}

